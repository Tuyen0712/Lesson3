string caesarCipher(string s, int k) {
for(int i=0;i<s.length();i++){
    if (s[i]>='a' && s[i]<='z'){
        s[i]='a' + (s[i]-'a'+ k)%26;
    }else if (s[i]>='A'&& s[i]<='Z'){
        s[i]='A'+ (s[i]-'A'+k)%26;
    }
    }
    return s;
}

